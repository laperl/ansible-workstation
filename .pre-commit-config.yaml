---
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: ansible-lint
        name: ansible-lint
        entry: ansible-lint
        language: system
        types: [yaml]
      - id: yamllint
        name: yamllint
        entry: yamllint
        language: system
        types: [yaml]
